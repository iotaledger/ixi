plugins {
    id 'java'
    id 'idea'
}

group 'org.iota'
version '0.4-SNAPSHOT'

sourceCompatibility = 1.7
repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

dependencies {
    compile 'com.github.iotaledger:ict:27a1450ac4cfa84dd7fad83f385a250e4722e63b'
}

task ixi(type: Jar) {
    archiveName = 'ixi.jar'
    destinationDir = file("$rootDir")
    from("./") { include 'module.json' }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}