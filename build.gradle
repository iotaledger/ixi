plugins {
    id 'java'
    id 'idea'
}

group 'org.iota'
version '0.4-SNAPSHOT'

sourceCompatibility = 1.7
repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

dependencies {
    compile 'com.github.iotaledger:ict:ad34781f4370019d0ea04dc1a84b00cb8df447f3'
}

task ixi(type: Jar) {
    archiveName = 'ixi.jar'
    destinationDir = file("$rootDir")
    from("./") { include 'module.json' }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}